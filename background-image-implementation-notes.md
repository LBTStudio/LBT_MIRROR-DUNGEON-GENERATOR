# 背景画像機能の実装・検証記録

## 反復作業の契約

| 項目 | 内容 |
| --- | --- |
| 目的 | ユーザーが任意の画像を鏡ダンジョンの移動マップの背景として読み込み、マスや結線の背面で位置・拡大率を直感的に調整できるようにする。 |
| 制約 | 完全にブラウザ内で処理し、外部送信・ログイン・課金・アカウント作成を不要とする。既存のマップ移動、ピンチ／ホイール拡縮、ノード選択、結線、JSON入出力を維持する。 |
| 範囲 | 背景の画像読込、表示／非表示、ドラッグ位置調整、倍率、初期化、ローカル保存、JSON保存・読込、PNG／JPEG出力。画像を外部に保存・共有する機能は対象外。 |
| 完了根拠 | デスクトップと390×844相当モバイルで、アップロード→表示→調整→保存／復元→出力の流れを操作し、画像がマップ要素より背面で表示されることを確認する。 |
| 修正上限 | 同一課題は観察後に最大2回の局所修正とし、追加の仕様判断が必要なら停止して確認する。 |

## 基準状態（実装前）

| 観点 | 観察結果 | 設計への反映 |
| --- | --- | --- |
| タスク準備性 | PC版は上部ツールバー、キャンバス右上の表示倍率、左下ミニマップ、左右のフローティング編集パネルで構成される。 | 背景機能は高頻度のツールバーへ短い入口を追加し、詳細調整は必要時のみ開く。 |
| 情報構造 | マップ要素の編集はキャンバス／フローティングパネル、保存は既存の「保存」メニューに集約されている。 | 背景はマップ全体の表示要素として、テーマ操作の近くに「背景」入口を置く。JSON／画像出力には既存の保存フローを使う。 |
| 信頼と継続性 | 現在のJSONセーブはマップ状態全体を保存し、画像出力は描画済みSVGをラスタライズする。 | 画像データは明示的にJSONに含め、サイズ・形式を検査して失敗時は既存状態を変更しない。 |
| レスポンシブ操作 | 390×844相当ではツールバーが折り返され、キャンバス空白の1本指ドラッグはパン、2本指は拡縮、下部シートが編集に使われる。 | 背景調整中だけキャンバスの1本指ドラッグを背景移動に切り替え、通常のパン操作を失わないよう、調整モードの状態を常に表示する。 |

## 実装後のPC一次確認

背景の入口は既存の「テーマ」と「保存」の間に収まり、スクリーンショット上でツールバーの折返しやキャンバス右上の倍率操作との重なりは確認されなかった。入口を開くと、画像形式とローカル処理の説明、画像選択ボタンが幅296pxのポップオーバー内に表示され、ノードや左側のマス種類パレットを覆わない。DOMでも、画像ファイル入力がPNG・JPEG・WebPに限定されていることを確認した。次の検証では実画像を投入し、背景レイヤー、ドラッグ、保存・出力を確認する。

実画像（WebP）を入力すると、893×768pxへ正規化された背景情報、表示切替、位置調整、倍率、不透明度、初期化、削除が同一パネルへ展開された。スクリーンショットでは、画像が経路・ノード・ラベルより背面にあり、調整モードの状態表示と保存完了通知も確認できた。合成ポインタイベントでは背景位置の反映が同フレーム内に取得できなかったため、次は再描画後の属性と保存値を確認して判定する。

再描画後のPCスクリーンショットでは、背景内の要素が右上方向へ移動し、ノード・経路・ラベルが前面のまま読み取れることを確認した。背景の保存値は `x: 86.89`、`y: -42.43`、SVGの描画位置は元画像を中央に置く計算を含め `x: 226.39`、`y: -118.43` となり、差分が保存値と一致した。したがって、背景だけのドラッグ調整とローカル自動保存は通過と判定する。

倍率を145%、不透明度を26%へ変更したスクリーンショットでは、背景が適切に拡大されながらノード枠・経路・ラベルの前面性を維持した。操作パネルの数値表示も入力値と一致し、パレット・倍率コントロール・右下の編集入口に重なりはなかった。背景画像そのものが高コントラストな画面キャプチャであっても、低不透明度では主マップの情報を読めるため、可読性を損なわない調整余地が確認できた。

## 実装後の390px相当モバイル確認

背景が設定済みの状態でも、ツールバーは390px枠内で折り返され、背景の存在を示す印と「保存」操作を確認できた。背景パネルは画面幅に収まり、各入力がタップ可能な間隔で表示された。一方で、位置調整を開始した後も詳細パネルを開いたままでは、調整対象の上部マップを覆い続けるため、位置調整ボタンを押した時点でパネルを自動的に閉じ、上部ツールバーの強調とキャンバス上の調整中表示だけを残す局所修正を加えた。これにより、既存の下部シートの構造を変えずに背景ドラッグの対象領域を確保する。

修正後の390px相当スクリーンショットでは、位置調整ボタンを押すと詳細パネルが閉じ、中央上部に「背景調整中」の小さな状態表示だけが残った。マップのノード領域・左下のマス入口・右下の編集入口はいずれも再び露出しており、モバイルで背景位置を調整するための空白領域を確保できている。

背景設定済みのPC画面で既存の保存メニューを開いたところ、PNG・JPEG・JSONセーブ・JSON読込の導線は以前と同じ順序で表示された。メニューは背景画像やノード操作へ常時干渉せず、背景機能が保存機能の情報構造を置き換えていないことを確認した。

PNG保存ダイアログは既存の背景選択・解像度倍率の構造を維持しており、背景設定済みのマップでも表示崩れはなかった。テーマ背景・2倍を選択して保存を実行すると、完了通知「PNGを保存しました」が表示され、背景データURLを含むSVGから画像出力までのブラウザ経路がエラーなく完了した。次はダウンロード済みPNGを直接確認する。

ダウンロード済みPNGは2344×1232px・RGBA形式で出力され、容量は約898KBだった。画像を直接目視すると、背景画像はステージ見出し・経路・ノード・ラベルの背面に保持され、主マップは前面で明瞭に読み取れる。画面上の操作コントロールや編集中の選択表示は出力に含まれず、背景を含む画像出力は通過と判定する。

既存の保存メニューからJSONセーブを実行すると、背景設定済みのまま「JSONセーブファイルを保存しました」という完了通知が表示された。次の検証では、出力ファイルの背景オブジェクトを直接確認した上で、同じファイルを読込して復元まで確認する。

出力JSONには背景オブジェクトが含まれ、画像データ、ファイル名、893×768pxの正規化寸法、ドラッグ後の位置、145%の倍率、26%の不透明度が記録されていることを直接確認した。JSON読込の隠し入力も `application/json` 専用であることを確認し、同じファイルを使った復元テストへ進む。

復元テストでは、ローカル自動保存を一時的に背景なしの初期マップへ切り替えて再読み込みした。スクリーンショット上でも背景印と背景描画が消え、通常マップのみの状態になっていることを確認した。この状態からJSON読込を行い、背景データが復元されるかを検証する。

背景なし状態で保存済みJSONを読み込むと、「JSONセーブを読み込みました」という完了通知とともに背景印・背景画像・先に調整した位置・倍率・不透明度が復元された。スクリーンショットでも背景が主マップの背面へ戻っているため、JSON保存・読込の往復は通過と判定する。

## 最終回帰

ブラウザコンソールにはReact開発版とブラウザ内Babelに関する既知の情報・警告以外の実行時エラーはなかった。自動テストでは、初期化、正規化、巨大／不正データの無効化、変形初期化、非対応形式の拒否、上限超過画像の拒否を含む6件がすべて通過した。PCと390×844相当モバイルの複数スクリーンショット、実画像アップロード、ドラッグ、倍率・不透明度変更、PNG出力、JSONの保存・読込を組み合わせ、背景機能と既存マップ編集の主要導線に重なり・切れ・操作遮蔽・状態不明瞭がないことを確認した。

## 三観点の統合判断

「タスク準備性」「情報構造」「レスポンシブ操作」の三観点では、背景を常時ドラッグ可能にすると、既存のパン・ノード・結線操作と衝突する点が共通の主要リスクである。そのため、背景表示の近くにある明示的な**調整モード**を入口にし、解除時は既存のキャンバス操作を完全に保持する。背景画像は最背面のSVGレイヤーとし、マップの可読性を保つため不透明度も調整可能とする。

## 実装設計

| 要素 | 採用する設計 | 理由 |
| --- | --- | --- |
| 状態 | `map.background` に、ローカルのラスターデータURL、画像寸法、ファイル名、表示状態、位置、倍率、不透明度を保持する。 | 既存のJSONセーブと履歴に同じ経路で含められ、外部保存を必要としない。 |
| 入力 | PNG・JPEG・WebPだけを受け付け、ブラウザ内で最大辺1920px・概ね1.5MB以下のラスターデータに縮小・圧縮する。 | SVGなどの実行可能コンテンツを除外し、localStorageとJSONセーブで安定して復元できる実用的なサイズへ収める。元の画像は送信しない。 |
| 配置 | 画像はSVGのテーマ背景矩形より上、列ガイド・経路・ノードより下に描画する。 | 既存の読みやすさとノード／経路のクリックを保つ。 |
| 調整 | 「背景」ポップオーバーで明示的に調整モードをオンにし、その間だけキャンバス空白の1本指／左ドラッグを背景移動に割り当てる。倍率はスライダー、初期化・表示／非表示・削除を同じ箇所に置く。 | 常時ドラッグによる既存パン・ノード選択・結線との競合を防ぐ。モバイルでも状態が明示される。 |
| 保存／出力 | ローカル自動保存・JSON保存／読込・PNG／JPEG出力に背景を含める。出力の「透過」はテーマ背景だけを透明化し、背景画像そのものは残す。 | 利用者が調整した地図画像を、そのまま再利用できる。 |

## 背景枠の自由変形 — 反復作業の契約

| 項目 | 内容 |
| --- | --- |
| 目的 | 背景調整モードで画像の枠を直接押し、枠内のドラッグで移動、四隅・四辺のハンドルで幅・高さを自由に変更できるようにする。 |
| 維持するもの | 通常時のパン、ホイール／ピンチ拡縮、ノード選択、結線、既存の背景位置ドラッグ、ローカル保存、JSON保存／読込、PNG／JPEG出力。 |
| 操作境界 | 枠とハンドルは背景調整モード中だけ表示する。ノード・結線・ツールバー・ミニマップの上では既存操作を優先し、通常モードでは背景枠をクリック対象にしない。 |
| 保存モデル | 既存の`scale`を互換用の基準倍率として維持し、横軸・縦軸を独立操作する`scaleX`・`scaleY`を追加する。旧JSONは`scale`を両軸へ補完する。 |
| 受入条件 | 枠内移動、四隅の二軸変形、四辺の一軸変形、最小／最大寸法の安全停止、初期化、保存復元、PNG出力、PC・390px相当の状態表示を実操作とスクリーンショットで確認する。 |
| 修正上限 | 枠の到達性・重なり・既存操作の競合は、実装後に最大2回の局所修正で判定する。 |

## 三観点の統合判断 — 背景枠の自由変形

| 観点 | 観察・リスク | 採用する局所解 |
| --- | --- | --- |
| タスク準備性 | 現行は空白ドラッグと倍率スライダーを往復するため、画像の端を合わせる作業では結果の見通しが弱い。 | 画像と同じ矩形に破線枠、中央の移動面、四隅・四辺のハンドルを表示する。ハンドルを押した時点で操作の種類を固定する。 |
| 情報構造・安全性 | 背景を常時クリック可能にすると、ノード選択・結線・パンが不安定になる。 | 枠は既存の明示的な背景調整モード中だけ有効にし、マス・結線・固定コントロールは最前面の既存イベントを維持する。 |
| レスポンシブ操作 | 390px相当ではマップが縮小表示され、PC向けの小さな角ハンドルでは到達しにくい。 | モバイルではハンドルの当たり領域を大きくし、背景パネルは調整開始時に閉じる既存仕様を維持する。キャンバス上の状態表示を「枠内移動／角・辺で幅高さ」と具体化する。 |

> 保存値は表示枠の幅・高さを別々に保持するのではなく、元画像寸法に対する **`scaleX` と `scaleY`** とする。これにより旧データの`scale`との互換性、リセット、出力時のSVG描画を同じ経路で保てる。

## 自由変形 — PC一次確認

- 基準状態: `/home/ubuntu/screenshots/127_0_0_1_2026-08-21_14-47-48_8206.webp`。均等倍率55%の背景では、破線枠と四隅・四辺の8ハンドルが画像と同じ矩形に表示され、既存のノード、結線、マスパレット、ミニマップ、倍率操作より背面に保たれる。背景パネルには元画像寸法と現在の枠寸法（px）を併記した。
- 右下ハンドルを実ポインタイベントで`+120px / +70px`ドラッグすると、表示枠は491×422pxから612×493pxへ変化した。スクリーンショットは`/home/ubuntu/screenshots/127_0_0_1_2026-08-21_14-48-41_6188.webp`。枠・ハンドルは残り、経路・ノード・ラベルは前面のまま読める。
- 右辺ハンドルを`+80px / 0px`ドラッグした保存値は`scaleX: 0.776281...`、`scaleY: 0.642088...`となった。縦軸は維持され、横軸のみが変わることを確認した。基準倍率`scale`は幾何平均の0.706として更新され、既存の均等倍率スライダーと矛盾しない。
- 自動保存キー`kagami-map-studio.v3`には、位置`x/y`、互換用`scale`、自由変形用`scaleX/scaleY`が記録された。これは画像データを外部へ送信せず、既存のローカル保存経路だけを使う。

## GIF・APNG・低FPSアイコンアニメーション — 設計

| 対象 | 採用する設計 | 既存機能との境界 |
| --- | --- | --- |
| 背景読込 | GIF・APNGを入力候補へ追加し、ブラウザが展開した先頭時点のラスタを既存のWebP／JPEG静止背景へ正規化する。 | 保存するのは既存と同じ安全な静止データURLだけであり、アニメーション画像の原本・外部URL・実行可能コンテンツを保存しない。 |
| GIF出力 | マップを3-3-2パレットの実GIFとして生成する。アイコンアニメーション有効時は3フレーム・4FPS、無効時は静止1フレームとする。 | GIFの透明出力は扱わず、テーマ背景または白背景へ合成する。PNG／JPEGの既存経路は変更しない。 |
| APNG出力 | 各SVGラスターフレームのPNG圧縮データをブラウザ内でAPNGチャンクへ組み立てる。アイコンアニメーション有効時は3フレーム・4FPS、無効時は静止1フレームとする。 | APNGはPNG同様に透過を選択できる。圧縮・出力とも端末内だけで実行する。 |
| アイコンモード | テーマ設定に`iconAnimation`を保存し、通常は静止、オン時だけすべてのマスアイコンを`steps(3)`による小さな上下移動・微回転へ切り替える。 | ノード選択、結線、パレット、詳細の静止アイコンは変えない。`prefers-reduced-motion`時は視覚アニメーションを止める。 |
| 自由変形モバイル | 透明な当たり領域を拡大し、視覚用の小さな角・辺ハンドルだけを枠上へ描画する。 | ノード・結線・下部シートの既存タップを優先し、背景調整モード外では枠を表示しない。 |

> GIF・APNGは、単に拡張子を変えるのではなく、ブラウザ内で実際のGIF／APNGバイナリとして生成する。アイコンの待機アニメーションをオンにしたときだけ、同じ低FPSの3フレームを出力へ反映する。

## 自由変形・画像形式・低FPSアイコン — 実装後の検証

| 検証対象 | 実操作・確認結果 | 判定 |
| --- | --- | --- |
| PC自由変形 | 枠内移動、右下角の二軸変形、右辺の横軸だけの変形を実操作し、`scaleX`・`scaleY`が独立してローカル保存されることを確認した。 | 通過 |
| 390px相当自由変形 | 当初は大きい背景の右端ハンドルが画面外へ出たため、調整開始時に枠の全体が収まる縮尺と中央位置へ補正する局所修正を行った。修正後は枠が`left: 18`〜`right: 370`、キャンバスが`left: 0`〜`right: 388`となり、八つのハンドルがすべて表示領域内へ収まった。 | 通過 |
| GIF背景読込 | 実出力した1172×616px GIFを背景として読み込み、名称を保持しながら保存データを`data:image/webp`の静止ラスタへ正規化することを確認した。 | 通過 |
| APNG背景読込 | 実出力した1172×616px APNGを背景へ読み込み、背景パネル・枠・状態通知で反映を確認した。 | 通過 |
| GIF出力 | 低FPSアイコンモードを有効化し、1×GIFを実出力した。生成ファイルはGIF89a、1172×616pxとして識別された。 | 通過 |
| APNG出力 | 同じ状態で1×APNGを実出力した。生成ファイルには`acTL` 1個、`fcTL` 3個、`fdAT` 70個が含まれ、3フレームのAPNGであることを確認した。 | 通過 |
| アイコン待機モード | テーマ設定から有効化後、マップ上の11アイコンだけに`0.75s steps(3)`の待機アニメーションが適用され、保存値`theme.iconAnimation: true`と一致した。 | 通過 |
| JSONセーブ | APNG背景の名称・`scaleX`・`scaleY`と、`theme.iconAnimation: true`が最新JSONセーブに記録されていることを直接確認した。 | 通過 |

PCと390px相当モバイルのスクリーンショットでは、保存メニューに追加したGIF・APNGの入口が既存のPNG・JPEG・JSONと同じ局所ポップオーバーに収まり、常時のツールバー密度を変えないことを確認した。ブラウザコンソールに確認されたのはReact開発版とブラウザ内Babelの既知の情報・警告のみで、新機能に起因する実行時エラーはなかった。追加したバイナリ形式・自由変形・UI入口・背景正規化を含む自動テスト14件はすべて通過した。
