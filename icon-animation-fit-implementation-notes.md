# アイコン特性別アニメーション・背景初期フィット配置 — 実装・検証記録

## 反復作業の契約

| 項目 | 内容 |
| --- | --- |
| 目的 | ノード種別の形状・役割に沿った低FPS待機モーションを与え、アップロード背景の全体を初期状態でマップ表示域へ収める。 |
| 維持する制約 | 通常表示、`prefers-reduced-motion`、パン、ピンチ／ホイール拡縮、ノード選択・結線、背景自由変形、ローカル保存、JSON、GIF・APNG出力を維持する。 |
| 直接証拠 | 同じ既定マップでPC幅と390×844相当の画面を操作・撮影し、背景フレーム、種別別アイコン、出力フレームを確認する。 |
| 合格条件 | 新規背景は初期状態で表示枠の全辺がマップ実表示域内にあり、中央へ配置される。低FPSモードでは異なる役割のアイコンが少なくとも別の3段階モーションを使い、通常表示・軽減モーションでは静止する。 |
| 修正上限 | 同一原因に対して最大2回の局所修正を行い、既存操作の優先順位を変える必要があればユーザー判断を求める。 |

## 改修前の観察

PC幅の既定マップで、テーマ設定に「アイコンを低FPSアニメ調にする」切替があり、既存の実装では全アイコンに同じ上下移動・微回転が付くことを確認した。アイコンは開始点、一般戦闘、集中戦闘、精鋭戦闘、幻想体、イベント、ショップ、ボス戦闘という異なるシルエットと役割を持つため、この一律モーションでは役割を補助できない。

背景読込はPNG・JPEG・WebP・GIF・APNGに対応するが、読込直後の背景値は倍率1・中心オフセット0で作られる。したがって、画像の正規化後寸法がマップ表示域より大きい場合、初期状態で全体が収まることを保証していない。

## 採用方針

| 対象 | 採用する局所設計 |
| --- | --- |
| 一般・開始点・イベント | 小さな縦移動または呼吸のような振幅に留め、経路探索・発見の静かな待機感を持たせる。 |
| 戦闘・集中戦闘 | ごく短い前後の重心移動または一方向の傾きにし、武器・戦闘アイコンの方向性を保つ。 |
| 精鋭・幻想体・ボス | 回転を強めず、警戒シルエットを崩さない小さな押し出し・脈動に留める。 |
| ショップ | 箱・屋根の水平性を崩さない、短い上下の開閉感に留める。 |
| 背景初期配置 | 正規化済み画像の幅・高さとマップレイアウト幅・高さからcontain倍率を求め、余白を確保した中央初期配置にする。初回アップロード後も自由変形と均等倍率の明示操作は変更しない。 |

## 実装・検証結果

| 観点 | 実測・観察 | 判定 |
| --- | --- | --- |
| PC背景初期配置 | 1600×900pxの横長PNGを通常の画像選択経路で読込んだ。自動計算された枠は967×544px、均等倍率は60%で、中央オフセットは0として表示された。枠、ノード、結線、自由変形ハンドルは同時に表示され、ノード・結線は前面に維持された。 | 通過 |
| 種別別モーション | 実画面の計算済みスタイルで、開始点・一般戦闘・イベント・集中戦闘・精鋭・幻想体・ショップ・ボスが、それぞれ対応する`icon-idle-*`アニメーション名、0.75秒、3段階ステップを持つことを確認した。 | 通過 |
| GIF出力 | アイコンアニメーション有効状態で2×GIFを実行し、「GIFを保存しました（4FPS）」の通知を確認した。生成物はGIF89a、2344×1232px、3個のフレーム制御ブロックを持つ。 | 通過 |
| 390×844相当 | 背景枠は317×190px、マップSVGは354×186pxとして表示され、横オーバーフローはなかった。種別別の8種類のアニメーション名もモバイルの計算済みスタイルで確認した。 | 通過 |
| 自動テスト | 背景ユーティリティ9件、自由変形・UI6件、GIF/APNG2件の全17件が通過した。 | 通過 |

通常表示を選ぶ場合と`prefers-reduced-motion: reduce`の場合は、既存どおりアイコンの待機アニメーションを停止する。背景初期フィットは新規読込時だけに適用され、その後の枠内移動、角・辺の自由変形、均等倍率、パン、ピンチ拡縮、ノード選択・結線の導線は変更していない。
